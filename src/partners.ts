'use strict';
import { ExtensionContext } from 'vscode';

export function registerPartnerActionRunners(_context: ExtensionContext): void {
	// Partner integrations (Live Share, etc.) removed in the Search & Compare-only fork.
}
